/**
 * WordPress dependencies
 */
import { title, list, postDate } from '@wordpress/icons';

export const titleCount = title;
export const titleDescription = list;
export const titleCountDescription = postDate;
