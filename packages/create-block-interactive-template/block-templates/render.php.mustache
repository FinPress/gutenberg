<?php
/**
 * PHP file to use when rendering the block type on the server to show on the front end.
 *
 * The following variables are exposed to the file:
 *     $attributes (array): The block attributes.
 *     $content (string): The block default content.
 *     $block (FP_Block): The block instance.
 *
 * @see https://github.com/FinPress/gutenberg/blob/trunk/docs/reference-guides/block-api/block-metadata.md#render
 */

// Generates a unique id for aria-controls.
$unique_id = fp_unique_id( 'p-' );

// Adds the global state.
fp_interactivity_state(
	'{{namespace}}',
	array(
		'isDark'    => false,
		'darkText'  => esc_html__( 'Switch to Light', '{{textdomain}}' ),
		'lightText' => esc_html__( 'Switch to Dark', '{{textdomain}}' ),
		'themeText'	=> esc_html__( 'Switch to Dark', '{{textdomain}}' ),
	)
);
?>

<div
	<?php echo get_block_wrapper_attributes(); ?>
	data-fp-interactive="{{namespace}}"
	<?php echo fp_interactivity_data_fp_context( array( 'isOpen' => false ) ); ?>
	data-fp-watch="callbacks.logIsOpen"
	data-fp-class--dark-theme="state.isDark"
>
	<button
		data-fp-on--click="actions.toggleTheme"
		data-fp-text="state.themeText"
	></button>

	<button
		data-fp-on--click="actions.toggleOpen"
		data-fp-bind--aria-expanded="context.isOpen"
		aria-controls="<?php echo esc_attr( $unique_id ); ?>"
	>
		<?php esc_html_e( 'Toggle', '{{textdomain}}' ); ?>
	</button>

	<p
		id="<?php echo esc_attr( $unique_id ); ?>"
		data-fp-bind--hidden="!context.isOpen"
	>
		<?php
			esc_html_e( '{{title}} - hello from an interactive block!', '{{textdomain}}' );
		?>
	</p>
</div>
